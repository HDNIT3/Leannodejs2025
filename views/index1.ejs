<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Styled H1 List</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f4f4f9;
      }

      .container {
        width: 60%;
        background-color: #ffffff;
        border-radius: 15px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        padding: 20px;
      }

      .container h1 {
        margin: 15px 0;
        padding: 10px;
        text-align: center;
        background-color: #0078d7;
        color: white;
        border-radius: 10px;
        font-size: 18px;
        transition: transform 0.3s ease, background-color 0.3s ease;
      }

      .container h1:hover {
        background-color: #0053a3;
        transform: scale(1.05);
      }

      .create-button {
        position: absolute;
        top: 20px;
        right: 20px;
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 5px;
        padding: 10px 15px;
        font-size: 14px;
        cursor: pointer;
        transition: background-color 0.3s ease;
        text-decoration: none;
      }

      .create-button:hover {
        background-color: #218838;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <% hello.forEach((x)=>{ %>
        <a href="product/detail/<%= x.id %>">
          <h1><%= x.name %> and ID = <%= x.id %></h1>
        </a>
      <% }) %>
    </div>
    <a href="product/create" class="create-button">Create</a>
  </body>
</html>
